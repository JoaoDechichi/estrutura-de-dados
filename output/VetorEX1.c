#include <stdio.h>

int main(){

    //Declarando e inicializando um vetor de inteiros

    int numeros[5] = {1, 2, 3, 4, 5};

    //Declarando uma string (vetor de caracteres)

    char  nome[10] = "Exemplo";

    printf("\nTerceiro elemento do vetor: %d\n", numeros[2]);
    printf("\nPrimeiro letra do nome: %c", nome[0]);
    
    return 0;
}